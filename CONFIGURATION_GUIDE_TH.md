# คู่มือการตั้งค่าชื่อโรงเรียนและฐานข้อมูล (สำหรับผู้ดูแลระบบ)

เอกสารฉบับนี้จะแนะนำวิธีการเปลี่ยน **ชื่อโรงเรียน** และ **แยกฐานข้อมูล** เพื่อให้คุณสามารถนำระบบ KruKit Premium Global ไปใช้งานกับโรงเรียนของคุณเองได้อย่างถูกต้อง

---

## 1. ไฟล์การตั้งค่า (.env) คืออะไร?
ไฟล์ `.env` เปรียบเสมือน **"แผงควบคุม"** ของระบบ ที่ช่วยให้คุณปรับเปลี่ยนข้อมูลสำคัญได้โดยไม่ต้องเข้าไปแก้ไขโค้ดที่ซับซ้อน เพียงแค่แก้ไขข้อความในไฟล์นี้ ระบบก็จะทำงานเปลี่ยนไปตามที่คุณกำหนด

## 2. ขั้นตอนการตั้งค่า

### ขั้นตอนที่ 1: เปิดหรือสร้างไฟล์ .env
1. ที่โฟลเดอร์หลักของโปรเจกต์ (ระดับเดียวกับไฟล์ `package.json`) ให้มองหาไฟล์ชื่อ `.env`
2. หากไม่มี ให้สร้างไฟล์ใหม่และตั้งชื่อว่า `.env` (มีจุดข้างหน้า และไม่มีนามสกุลไฟล์)

### ขั้นตอนที่ 2: กำหนดค่า
เปิดไฟล์ `.env` และเพิ่มโค้ดการตั้งค่า 2 บรรทัดนี้ลงไป:

```env
# 1. ชื่อโรงเรียนที่จะแสดงบนหน้าเว็บและในเอกสารใบงาน/PDF
VITE_SCHOOL_NAME=ระบุชื่อโรงเรียนของคุณที่นี่

# 2. รหัสประจำตัวโรงเรียน (ภาษาอังกฤษ ห้ามเว้นวรรค) เพื่อใช้แยกฐานข้อมูล
VITE_FIRESTORE_APP_ID=my-school-id
```

#### ตัวอย่างการกรอกข้อมูล
สมมติว่าโรงเรียนของคุณชื่อ **"โรงเรียนอนุบาลหมีน้อย"**
```env
VITE_SCHOOL_NAME=โรงเรียนอนุบาลหมีน้อย
VITE_FIRESTORE_APP_ID=meenoi-kindergarten
```

### คำอธิบายตัวแปร
*   **VITE_SCHOOL_NAME**:
    *   ใส่ชื่อโรงเรียนของคุณได้เลย (ภาษาไทยหรืออังกฤษก็ได้)
    *   ชื่อนี้จะไปปรากฏที่: แถบหัวเว็บ, หน้า Print ใบงาน, หัวกระดาษ PDF และรายงานบันทึกสุขภาพ
*   **VITE_FIRESTORE_APP_ID**:
    *   ตั้งชื่อรหัสภาษาอังกฤษสั้นๆ (เช่น `school-a`, `demo-school`)
    *   **สำคัญ**: การเปลี่ยนรหัสนี้ จะทำให้ระบบ **"สร้างกระดานข้อมูลใหม่"** ทันที ข้อมูลนักเรียนหรือคะแนนเก่าๆ จากรหัสอื่นจะไม่ตามมา (เหมือนเริ่มโรงเรียนใหม่) ซึ่งช่วยให้ข้อมูลไม่ปะปนกัน

---

## 3. การบันทึกและเริ่มใช้งาน
หลังจากแก้ไขไฟล์ `.env` เสร็จแล้ว ให้ทำตามนี้เพื่อให้การตั้งค่ามีผล:

1.  บันทึกไฟล์ (Save)
2.  **รีสตาร์ทเซิร์ฟเวอร์ (Restart Server)**
    *   ไปที่หน้าจอดำๆ (Terminal/Command Prompt) ที่รันโปรเจกต์อยู่
    *   กดปุ่ม `Ctrl + C` เพื่อหยุด
    *   พิมพ์คำสั่งเดิม (เช่น `npm run dev`) แล้วกด Enter
3.  ตรวจสอบที่หน้าเว็บ ชื่อโรงเรียนควรจะเปลี่ยนเป็นชื่อใหม่ที่คุณตั้งทันที

---

## เคล็ดลับเพิ่มเติม (FAQ)

**Q: ถ้าอยากกลับไปใช้ข้อมูลตัวอย่าง (Banwanghin) ต้องทำอย่างไร?**
A: ทำได้ 2 วิธีครับ
1.  **ลบ 2 บรรทัดนี้ออกจากไฟล์ .env** (ระบบจะดีดกลับไปใช้ค่าเริ่มต้นอัตโนมัติ)
2.  หรือ ตั้งค่ากลับเป็นค่าดั้งเดิม:
    ```env
    VITE_SCHOOL_NAME=โรงเรียนบ้านวังหิน
    VITE_FIRESTORE_APP_ID=banwanghin-lms-dev
    ```

**Q: เปลี่ยนชื่อโรงเรียนแล้ว แต่ทำไมข้อมูลนักเรียนหายหมด?**
A: เพราะคุณเปลี่ยน `VITE_FIRESTORE_APP_ID` ครับ ระบบมองว่าเป็นโรงเรียนใหม่เลยสร้างฐานข้อมูลว่างๆ ให้ ถ้าอยากได้ข้อมูลเดิมต้องใช้ ID เดิมครับ

---
*หากมีข้อสงสัยเพิ่มเติม สามารถติดต่อผู้พัฒนาระบบได้ทันที*
